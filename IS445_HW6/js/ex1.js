//Complete the following functions
//Displays all body nodes
const bodyNodes = () => {
  const body = document.querySelector("body"); // Grab the first body tag
  console.log(body); 
}
bodyNodes();
